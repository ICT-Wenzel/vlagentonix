services:
  crewai:
    container_name: crewai_agent
    build:
      context: ./agent
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./agent:/app
      - /var/run/docker.sock:/var/run/docker.sock